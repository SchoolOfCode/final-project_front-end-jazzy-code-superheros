import Button from "../Button";

const Home = () => {
  return(
    <div className="home">
      <Button nameButton="Get Help"/>
      <Button nameButton="Give Help"/>
    </div>
  
  );
};

export default Home;
